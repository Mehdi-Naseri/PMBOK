@model IEnumerable<Pmbok.ViewModels.ViewModels.ProjectDocumentFileDeletedViewModel>

@foreach (var file in Model)
{
    <div class="meh-documentFile">
        @Html.ActionLink(file.FileName, "DownloadDocumentFileDeleted", new { id = file.Id }, null)
    </div>
}
